sap.ui.define([
    'benit/ui5/BaseController'
], function (BaseController) {
    'use strict';
    return BaseController.extend('swx.app.zbnt257test.controller.List', {
        // onInit: function () {
        //     BaseController.prototype.onInit.apply(this, arguments);
        //     this.getRouter().getRoute('default').attachMatched(this._onRouteMatched, this);
        // },
        
        // _onRouteMatched : function(oEvent) {
        // },

/*
        onSearch : function(oEvent) {
            this.searchData();
		},

        onPressCreate: function(oEvent) {
            this.createData();
        },

        onPressUpdate: function(oEvent) {
            this.updateData();
        },

        onPressDelete: function(oEvent) {
            this.deleteData();
        },

        onPressChangeStatus: function(oEvent) {
            this.changeViewEditable();
        },

        onPressSettings: function(oEvent) {
            this.openVariantPopup();
        },

        onPressFavoriteButton: function(oEvent) {
            this.updateFavoriteApp();
        } 
*/

    });
});